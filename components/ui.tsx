"use client";
import { cn } from "@/lib/utils";
import { Plus, Sparkles, X } from "@/components/icons";
import { useUIStore } from "@/stores/ui-store";
export function Avatar({ initials, color = "bg-brand-500" }: { initials: string; color?: string }) { return <span className={cn("grid h-7 w-7 place-items-center rounded-full border-2 border-white text-[9px] font-bold text-white", color)}>{initials}</span>; }
export function Badge({ children, color = "bg-slate-100 text-slate-600" }: { children: React.ReactNode; color?: string }) { return <span className={cn("rounded-md px-2 py-1 text-[10px] font-bold", color)}>{children}</span>; }
export function NewTaskButton() { const open = useUIStore(s => s.openTask); return <button onClick={open} className="inline-flex items-center gap-2 rounded-xl bg-brand-500 px-4 py-2.5 text-sm font-semibold text-white shadow-lg shadow-brand-500/20 transition hover:bg-brand-600"><Plus size={17}/> Nova tarefa</button>; }
export function TaskModal() { const { taskOpen, closeTask } = useUIStore(); if (!taskOpen) return null; return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/30 p-4 backdrop-blur-sm"><div className="w-full max-w-lg rounded-2xl bg-white p-6 shadow-2xl"><div className="mb-6 flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-widest text-brand-500">TaskHub</p><h2 className="mt-1 text-xl font-bold">Criar nova tarefa</h2></div><button onClick={closeTask} className="rounded-lg p-2 hover:bg-slate-100"><X size={19}/></button></div><input autoFocus placeholder="No que você está trabalhando?" className="w-full border-b border-slate-200 pb-3 text-lg font-medium outline-none placeholder:text-slate-300"/><div className="mt-5 grid grid-cols-2 gap-3"><select className="rounded-lg border border-slate-200 p-2.5 text-sm"><option>Em andamento</option><option>A fazer</option></select><select className="rounded-lg border border-slate-200 p-2.5 text-sm"><option>Prioridade média</option><option>Alta</option><option>Baixa</option></select></div><button onClick={closeTask} className="mt-6 flex w-full items-center justify-center gap-2 rounded-xl bg-brand-500 py-3 font-semibold text-white"><Sparkles size={16}/> Criar tarefa</button></div></div>; }
