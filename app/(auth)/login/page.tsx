"use client";
import Link from "next/link";
import { useState } from "react";
import { createClient } from "@/lib/supabase/client";
export default function LoginPage(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [message,setMessage]=useState('');async function login(e:React.FormEvent){e.preventDefault();try{const {error}=await createClient().auth.signInWithPassword({email,password});setMessage(error?.message||'Login efetuado. Redirecionando...')}catch{setMessage('Configure as credenciais Supabase no .env.')}} return <main className="grid min-h-screen place-items-center bg-[#f8f8fb] p-5"><section className="w-full max-w-md rounded-2xl border border-slate-100 bg-white p-8 shadow-card"><div className="mb-8 flex items-center gap-2"><span className="grid h-8 w-8 place-items-center rounded-[10px] bg-brand-500 font-black text-white">T</span><b className="text-xl">TaskHub</b></div><h1 className="text-2xl font-bold">Boas-vindas de volta</h1><p className="mt-2 text-sm text-slate-500">Entre para continuar construindo grandes coisas.</p><form onSubmit={login} className="mt-7 space-y-4"><input value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="E-mail profissional" className="w-full rounded-xl border border-slate-200 px-4 py-3 text-sm outline-none focus:border-brand-400"/><input value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="Senha" className="w-full rounded-xl border border-slate-200 px-4 py-3 text-sm outline-none focus:border-brand-400"/><button className="w-full rounded-xl bg-brand-500 py-3 text-sm font-semibold text-white">Entrar</button></form><button className="mt-3 w-full rounded-xl border border-slate-200 py-3 text-sm font-semibold">Continuar com Google</button>{message&&<p className="mt-4 text-center text-xs text-slate-500">{message}</p>}<p className="mt-6 text-center text-xs text-slate-500">Não tem conta? <Link href="/signup" className="font-bold text-brand-500">Crie gratuitamente</Link></p><Link href="/forgot-password" className="mt-3 block text-center text-xs text-slate-400">Esqueci minha senha</Link></section></main>}
